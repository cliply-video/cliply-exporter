# deps
node_modules
# build output
dist
# rust
src-tauri/target
# tauri generated
src-tauri/gen/schemas
# downloaded binaries (resolved at runtime, never committed)
src-tauri/binaries
# env / local
.env
.env.local
.DS_Store
*.log
